# Gate.io 美股数字货币 MA10 趋势监控系统

## 一、项目简介

本项目是一个基于 **Gate.io U本位永续合约 API** 的 MA10（10周期移动平均线）趋势监控系统。系统支持 **Web 可视化面板** 和 **命令行脚本** 两种使用方式，可实时监控多个加密货币和美股代币的日K、4小时、60分钟、15分钟四个时间周期的 MA10 趋势方向（连续上涨 / 连续下跌 / 震荡）。

**核心特点：**
- 针对中国大陆网络环境优化（Gate.io 可直接访问，无需代理）
- 自动筛选可用标的，不可用的自动剔除
- 暗色主题响应式 Web 面板，手机/电脑均可使用
- 后台定时刷新 + 手动刷新双模式
- 零成本运行（Gate.io API 免费）

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 数据源 | Gate.io U本位合约 API | `https://api.gateio.ws/api/v4` |
| 后端 | Flask (Python) | 轻量 Web 框架，提供 REST API |
| 前端 | HTML5 + CSS3 + Vanilla JS | 暗色主题、响应式网格布局、内联样式（无外部 CDN 依赖） |
| 数据存储 | JSON 文件 + 内存缓存 | 标的信息持久化，监控数据内存缓存 |
| 网络库 | requests | Python HTTP 请求 |

---

## 三、文件结构

```
美股数字货币监控项目/
│
├── app.py                              # [核心] Flask Web 面板主程序
│                                       #   - 启动命令: python app.py
│                                       #   - 访问地址: http://127.0.0.1:5000
│                                       #   - 局域网: http://电脑IP:5000
│
├── gateio_available_symbols.json       # [配置] 32个可用标的列表
│                                       #   - 由 gateio_futures_scan.py 自动生成
│                                       #   - 包含: user_symbol, contract, last, change_percentage
│
├── gateio_futures_monitor.py           # [脚本] 命令行版完整监控
│                                       #   - 直接在终端输出所有标的的 MA10 趋势
│                                       #   - 适合不启动 Web 服务时使用
│
├── gateio_futures_scan.py              # [工具] Gate.io 合约市场标的扫描器
│                                       #   - 输入用户标的列表，自动检测哪些在 Gate.io 可用
│                                       #   - 生成 gateio_available_symbols.json
│
├── requirements.txt                    # Python 依赖清单
│                                       #   - flask >= 2.0.0
│                                       #   - requests >= 2.25.0
│
├── CLAUDE.md                           # 本文件，项目文档
│
# --- 以下为测试/备用/历史脚本，非核心运行依赖 ---
│
├── gateio_ma10_monitor.py              # [历史] Gate.io 现货版监控脚本（早期版本，仅支持现货市场）
├── binance_ma10_monitor.py             # [备用] Binance 版监控脚本（中国大陆网络不可访问，备用）
├── finnhub_monitor.py                  # [备用] Finnhub 美股监控（免费版无 K 线数据）
├── finnhub_test.py                     # [测试] Finnhub API 稳定性测试
├── test_apis.py                        # [测试] 多交易所 API 在中国大陆可达性测试
├── test_binance_final.py               # [测试] Binance API 最终连通性验证
├── test_futures_kline.py               # [测试] Gate.io 合约 K 线返回格式验证
├── test_gateio_futures.py              # [测试] Gate.io 合约市场美股标的可用性测试
├── test_symbols.py                     # [测试] 标的可用性测试（无下划线格式）
└── test_symbols2.py                    # [测试] 标的可用性测试（下划线格式，最终版）
```

---

## 四、快速开始

### 4.1 安装依赖

```powershell
pip install -r requirements.txt
```

或手动安装：

```powershell
pip install flask requests
```

### 4.2 启动 Web 面板（推荐）

```powershell
python app.py
```

启动后控制台会输出访问地址：

```
访问地址:
  本机: http://127.0.0.1:5000
  局域网: http://192.168.x.x:5000
```

**电脑浏览器访问：** `http://127.0.0.1:5000`

**手机访问（同一 WiFi 下）：** 打开手机浏览器，输入 `http://192.168.x.x:5000`（以启动时打印的实际 IP 为准）

### 4.3 添加到手机主屏幕（像 App 一样使用）

- **iPhone Safari**：打开网页 → 底部分享按钮 → "添加到主屏幕"
- **安卓 Chrome**：打开网页 → 右上角菜单 → "添加到主屏幕"

### 4.4 命令行版（备用）

```powershell
python gateio_futures_monitor.py
```

直接在终端输出所有标的的 MA10 趋势分析，适合查看原始数据或排查问题。

---

## 五、Web 面板功能说明

### 5.1 页面布局

| 区域 | 内容 |
|------|------|
| **顶部栏** | 标题 / 最后更新时间 / 手动刷新按钮 / 自动刷新倒计时 |
| **统计栏** | 日K上涨/下跌 / 4小时上涨/下跌 / 60分钟上涨/下跌 / 15分钟上涨/下跌 |
| **卡片网格** | 每个标的一张卡片，桌面多列、手机单列 |

### 5.2 卡片内容

每张卡片包含：
- **左上**：标的名称（如 BTCUSDT）+ 持仓方向标记（多/空/无，点击切换）
- **右上**：最新价 + 24h 涨跌幅（绿色涨 / 红色跌）
- **下方四格**：日K / 4小时 / 60分钟 / 15分钟

### 5.3 颜色编码

| 趋势状态 | 背景色 | 文字色 | 说明 |
|----------|--------|--------|------|
| 连续上涨 | 深绿色 | 绿色 | MA10 连续 >=3 周期上涨 |
| 连续下跌 | 深红色 | 红色 | MA10 连续 >=3 周期下跌 |
| 短期上涨 | 浅绿色 | 绿色 | MA10 1-2 周期上涨 |
| 短期下跌 | 浅红色 | 红色 | MA10 1-2 周期下跌 |
| 震荡 | 灰色 | 灰色 | MA10 无明显方向 |
| 数据不足 | 灰色 | 灰色 | K 线数据不足 20 根 |

### 5.4 刷新机制

- **自动刷新**：每 5 分钟（300秒）后台自动从 Gate.io 获取最新数据
- **手动刷新**：点击右上角"刷新数据"按钮，约 45 秒完成（32个标的 * 4次 API 请求）
- **缓存读取**：刷新期间前端仍显示旧数据，不会空白

---

## 六、API 端点

Web 面板提供以下 REST API：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 渲染主页面（HTML） |
| `/api/data` | GET | 获取当前监控数据（JSON） |
| `/api/refresh` | POST | 触发后台数据刷新（异步） |
| `/api/positions` | GET | 获取所有持仓标记 |
| `/api/position` | POST | 更新单个标的持仓标记（body: `{symbol, position}`） |

### `/api/data` 返回示例

```json
{
  "data": [
    {
      "symbol": "BTCUSDT",
      "contract": "BTC_USDT",
      "last": 76965.8,
      "change_pct": 3.04,
      "intervals": [
        {
          "name": "日K",
          "interval": "1d",
          "trend": "连续下跌",
          "consecutive": 10,
          "ma10": 77264.17,
          "close": 76965.8,
          "deviation": -0.39,
          "candles_count": 50
        }
      ]
    }
  ],
  "last_update": "2026-05-24 18:06:37",
  "updating": false,
  "error": null
}
```

---

## 七、当前监控标的（32个）

### 加密货币（23个）
BTC、ETH、SOL、HYPE、ZEC、NEAR、CL（原油）、BSB、XRP、BEAT、DOGE、SUI、ONDO、GMT、IN、GRASS、XAU（黄金）、XAG（白银）、BILL、TAO、EDEN、WLD

### 美股/ETF 代币（9个）
MU、SNDK、SOXL、EWY、INTC、DRAM、CBRS、AMD、QCOM、TSM

### 不可用标的（已剔除）
1000PEPE、NVDA、QQQ、CRCL、MSTR、SPY、TSLA、GOOGL、COIN、NATGAS、AMZN —— Gate.io 未上线这些合约

---

## 八、关键注意事项

### 8.1 数据来源与准确性
- **Gate.io 合约价格 ≠ 真实股价**：美股代币合约（如 AMD_USDT）存在 **2-4% 的系统性溢价**
- **趋势方向可靠**：虽然绝对价格有溢价，但涨跌趋势与真实美股高度一致
- **新合约数据不足**：BILL、SOXL、DRAM、CBRS、QCOM 等日K历史数据较少，需等数据积累

### 8.2 网络环境
- **无需代理**：Gate.io API 在中国大陆可直接访问
- **Binance/OKX/CoinGecko/Yahoo Finance 不可用**：已全面测试确认超时或封禁
- **局域网访问限制**：Web 面板默认绑定 `0.0.0.0`，同一 WiFi 下可访问；跨网络需公网 IP 或云服务器

### 8.3 API 限流
- Gate.io API 有频率限制，脚本已内置 **0.25秒请求间隔**
- 单次全量刷新约 128 次 API 请求，耗时约 45 秒
- 避免短时间内多次点击"刷新数据"

---

## 九、待办事项（按优先级排序）

### P0 - 核心功能扩展
- [ ] **技术指标扩展**：MA5 / MA20 / MA60 / MA120
- [ ] **金叉死叉信号**：MA5 上穿/下穿 MA10，MA10 上穿/下穿 MA20
- [ ] **MACD 指标**：DIF、DEA、MACD 柱状图
- [ ] **RSI 相对强弱指数**：6日/12日/24日 RSI
- [ ] **布林带**：上轨、中轨、下轨

### P1 - 体验优化
- [ ] **前端图表可视化**：集成 lightweight-charts，显示 K 线 + MA 线
- [ ] **历史数据持久化**：SQLite 本地数据库，减少 API 调用、支持回溯
- [x] **趋势转折告警**：当 MA10 从"连续下跌"转为"连续上涨"（或相反）时推送通知（含企业微信）
- [ ] **前端标的管理**：页面直接增删监控标的，无需修改 JSON 文件
- [x] **持仓方向标记**：卡片支持多/空/无三态切换，服务器端持久化
- [x] **预警快捷跳转**：点击顶部预警 badge 自动滚动定位到对应卡片

### P2 - 部署与通知
- [ ] **云服务器部署**：部署到香港/新加坡 VPS，实现 24h 随时访问
- [x] **推送通道**：企业微信机器人 Webhook 已接入
- [ ] **推送扩展**：钉钉/企业微信/Telegram Bot
- [ ] **定时任务**：Windows 任务计划程序 / Linux cron 自动运行
- [x] **自动启动浏览器**：运行脚本后数据刷新完成自动打开面板
- [x] **启动进度反馈**：命令行黑客风格启动画面 + 实时进度条

### P3 - 高级功能
- [ ] **多时间框架联动分析**：如"日线下跌 + 小时线上涨"的背离识别
- [ ] **多标的对比面板**：横向对比多个标的的趋势强度
- [ ] **数据导出**：Excel / CSV 导出监控结果
- [ ] **回测框架**：基于历史数据验证 MA10 策略有效性

---

## 十、常见问题

**Q: 为什么有些标的一直显示"数据不足"？**
A: BILL_USDT、SOXL_USDT、DRAM_USDT、CBRS_USDT、QCOM_USDT 是 Gate.io 近期新上线的合约，日K历史数据不足 20 根。等 1-2 周数据积累后自动正常。

**Q: 为什么 AMD_USDT 价格和真实 AMD 股价不一样？**
A: Gate.io 上的美股代币是**股权代币永续合约**，价格由 Gate.io 市场供需决定，并非直接锚定真实股票交易所。实测存在 2-4% 溢价，但趋势方向一致。

**Q: 可以增加 NVDA、TSLA、AMZN 吗？**
A: Gate.io 目前没有这些合约。如需监控，需使用能访问美股数据源的方案（Finnhub/Alpha Vantage），但这些 API 在中国大陆可能也有访问限制。

**Q: 电脑关机后手机还能访问吗？**
A: 不能。当前方案是电脑本地运行 Flask 服务。如需 24h 访问，需部署到云服务器（约 20-50 元/月的香港/新加坡 VPS）。

**Q: 如何修改自动刷新间隔？**
A: 打开 `app.py`，找到 `AUTO_REFRESH_INTERVAL = 300`，将 300 改为需要的秒数（如 60 表示每分钟刷新）。

---

## 十一、更新日志

| 日期 | 版本 | 内容 |
|------|------|------|
| 2026-05-24 | v1.0 | 初始版本：32个标的、3周期 MA10、Web 面板、命令行脚本 |
| 2026-05-24 | v1.1 | MA10 转折预警 + 企业微信通知 + 早期预警(<=3周期) + 涨跌幅计算 |
| 2026-05-25 | v1.2 | 4小时周期 + 持仓持久化 + 预警跳转 + 自动启动浏览器 + 黑客风格启动画面 + 进度条 + 端口清理 |

---

## 十二、联系与反馈

如需继续完善本项目，向 Claude 说明即可。建议直接说：
- "继续完善 Gate.io 监控项目"
- "帮我加 MACD 指标"
- "部署到云服务器"
- "加微信推送功能"
