# Gate.io 美股数字货币 MA10 趋势监控系统 v3.5.2

## 一、项目简介

本项目是一个基于 **Gate.io U本位永续合约 API** 的 MA10（10周期移动平均线）趋势监控系统。系统支持 **Web 可视化面板** 和 **命令行脚本** 两种使用方式，可实时监控 100 个加密货币和美股代币的日K、4小时、60分钟、15分钟四个时间周期的 MA10 趋势方向（连续上涨 / 连续下跌 / 震荡）。

**核心特点：**
- 针对中国大陆网络环境优化（Gate.io 可直接访问，无需代理）
- 成交额前 100 标的自动筛选
- **模块化架构**：app.py / monitor.py / alerts.py / state.py / templates/index.html 五模块
- **技术指标**：MA10 / SAR 趋势跟踪，SAR 方向连续周期计数
- **转折预警**：SAR↔MA10 双向验证框架，四周期独立，中途提前确认
- **K线图表**：lightweight-charts 交互式图表，支持 MA10 + SAR 指标叠加
- **SSE 实时推送** + 60s 轮询降级
- **SQLite K线缓存**，API 调用量减半
- 亮色/暗色主题响应式 Web 面板，手机/电脑均可使用
- 价格提醒、键盘快捷键、骨架屏加载
- 零成本运行（Gate.io API 免费）

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 数据源 | Gate.io U本位合约 API | `https://api.gateio.ws/api/v4` |
| 后端 | Flask (Python) | 轻量 Web 框架，提供 REST API + SSE |
| 前端 | HTML5 + CSS3 + Vanilla JS | 暗色/亮色双主题、响应式网格、lightweight-charts K线图 |
| 图表 | lightweight-charts 4.1.3 | TradingView 出品，CDN 加载 |
| 数据存储 | JSON 文件 + SQLite + 内存缓存 | 原子写入防损坏，K线本地缓存 |
| 网络库 | requests (连接池复用) | 线程级 Session，并发5线程 |

---

## 三、文件结构

```
美股数字货币监控项目/
│
├── app.py                              # [核心] Flask Web 面板 (~300行)
│                                       #   路由 + SSE + 启动逻辑
│
├── monitor.py                          # [核心] MonitorCore + 技术指标 (~340行)
│                                       #   数据获取、MA/MACD/RSI/布林带计算
│
├── alerts.py                           # [核心] 预警与通知 (~210行)
│                                       #   转折预警 SAR↔MA10 双向验证
│
├── state.py                            # [核心] 状态持久化 (~160行)
│                                       #   持仓/价格提醒/MA10状态/SQLite缓存
│
├── templates/
│   └── index.html                      # [核心] 完整前端 (~1400行)
│                                       #   暗色/亮色双主题、K线图模态框
│
├── gateio_available_symbols.json       # [配置] 100个可用标的列表
│
├── gateio_futures_monitor.py           # [脚本] 命令行版完整监控
│
├── gateio_futures_scan.py              # [工具] Gate.io 合约市场标的扫描器
│
├── requirements.txt                    # Python 依赖: flask + requests
│
├── README.md                           # 项目说明（面向用户）
├── CLAUDE.md                           # 本文件（面向 AI 开发者）
│
# --- 运行时生成文件 ---
├── klines.db                           # SQLite K线缓存
├── .ma10_state.json                    # 转折预警状态
├── positions.json                      # 持仓标记持久化
├── price_alerts.json                   # 价格提醒持久化
│
# --- 以下为测试/备用/历史脚本 ---
├── gateio_ma10_monitor.py              # [历史] 现货版监控（早期版本）
├── binance_ma10_monitor.py             # [备用] Binance 版（中国大陆不可用）
├── finnhub_monitor.py                  # [备用] Finnhub 美股监控
├── finnhub_test.py                     # [测试] Finnhub API 稳定性测试
├── test_apis.py                        # [测试] 多交易所可达性测试
├── test_binance_final.py               # [测试] Binance 连通性验证
├── test_futures_kline.py               # [测试] 合约K线返回格式验证
├── test_gateio_futures.py              # [测试] 合约市场美股标的可用性
├── test_symbols.py                     # [测试] 标的可用性测试
└── test_symbols2.py                    # [测试] 标的可用性测试（下划线格式）
```

---

## 四、快速开始

### 4.1 安装依赖

```powershell
pip install -r requirements.txt
```

或手动安装：

```powershell
pip install flask requests
```

### 4.2 启动 Web 面板（推荐）

```powershell
python app.py
```

启动后控制台会输出访问地址：

```
访问地址:
  本机: http://127.0.0.1:5000
  局域网: http://192.168.x.x:5000
```

**电脑浏览器访问：** `http://127.0.0.1:5000`

**手机访问（同一 WiFi 下）：** 打开手机浏览器，输入 `http://192.168.x.x:5000`（以启动时打印的实际 IP 为准）

### 4.3 添加到手机主屏幕（像 App 一样使用）

- **iPhone Safari**：打开网页 → 底部分享按钮 → "添加到主屏幕"
- **安卓 Chrome**：打开网页 → 右上角菜单 → "添加到主屏幕"

### 4.4 命令行版（备用）

```powershell
python gateio_futures_monitor.py
```

直接在终端输出所有标的的 MA10 趋势分析，适合查看原始数据或排查问题。

---

## 五、Web 面板功能说明

### 5.1 页面布局

| 区域 | 内容 |
|------|------|
| **顶部栏** | 标题 / 最后更新时间 / 手动刷新按钮 / 自动刷新倒计时 |
| **统计栏** | 日K上涨/下跌 / 4小时上涨/下跌 / 60分钟上涨/下跌 / 15分钟上涨/下跌 |
| **卡片网格** | 每个标的一张卡片，桌面多列、手机单列 |

### 5.2 卡片内容

每张卡片包含：
- **左上**：标的名称（如 BTCUSDT）+ 持仓方向标记（多/空/无，点击切换）
- **右上**：最新价 + 24h 涨跌幅 + 24h成交额（绿色涨 / 红色跌）
- **下方四格**：日K / 4小时 / 60分钟 / 15分钟

### 5.3 颜色编码

| 趋势状态 | 背景色 | 文字色 | 说明 |
|----------|--------|--------|------|
| 连续上涨 | 深绿色 | 绿色 | MA10 连续 >=3 周期上涨 |
| 连续下跌 | 深红色 | 红色 | MA10 连续 >=3 周期下跌 |
| 短期上涨 | 浅绿色 | 绿色 | MA10 1-2 周期上涨 |
| 短期下跌 | 浅红色 | 红色 | MA10 1-2 周期下跌 |
| 震荡 | 灰色 | 灰色 | MA10 无明显方向 |
| 数据不足 | 灰色 | 灰色 | K 线数据不足 20 根 |

### 5.4 刷新机制

- **自动刷新**：每 5 分钟（300秒）后台自动从 Gate.io 获取最新数据
- **手动刷新**：点击右上角"刷新数据"按钮，约 45 秒完成（32个标的 * 4次 API 请求）
- **缓存读取**：刷新期间前端仍显示旧数据，不会空白

---

## 六、API 端点

Web 面板提供以下 REST API：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 渲染主页面 |
| `/api/data` | GET | 监控数据（含指标、转折预警、价格提醒） |
| `/api/turning-points` | GET | 转折预警信号列表 |
| `/api/refresh` | POST | 触发后台数据刷新（异步） |
| `/api/positions` | GET | 获取持仓标记 |
| `/api/position` | POST | 更新持仓标记 (body: `{symbol, position}`) |
| `/api/stream` | GET | SSE 实时推送 |
| `/api/klines` | GET | K线数据 (?symbol=&interval=&limit=) |
| `/api/symbols` | GET/POST/DELETE | 标的管理 |
| `/api/price-alerts` | GET/POST/DELETE | 价格提醒管理 |

### `/api/turning-points` 返回示例

```json
{
  "data": [
    {
      "symbol": "BTCUSDT",
      "interval_name": "日K",
      "interval": "1d",
      "signal": "买入信号",
      "path": "SAR先翻转",
      "ma10_type": 2,
      "sar_direction": "bullish",
      "ma10_consecutive": 1,
      "sar_consecutive": 3,
      "close": 76965.8,
      "ma10": 77264.17,
      "timestamp": "2026-06-05 14:30:00"
    }
  ],
  "last_update": "2026-06-05 14:30:00"
}
```

### `/api/data` 返回示例

```json
{
  "data": [
    {
      "symbol": "BTCUSDT",
      "contract": "BTC_USDT",
      "last": 76965.8,
      "change_pct": 3.04,
      "intervals": [
        {
          "name": "日K",
          "interval": "1d",
          "trend": "连续下跌",
          "consecutive": 10,
          "ma10": 77264.17,
          "close": 76965.8,
          "open": 77200.0,
          "sar_trend": "连续下跌",
          "sar_consecutive": 8,
          "sar_flip": null,
          "sar_direction": "bearish",
          "candles_count": 50
        }
      ]
    }
  ],
  "last_update": "2026-06-05 14:30:00",
  "updating": false,
  "error": null,
  "turning_points": [
    {
      "symbol": "BTCUSDT",
      "interval_name": "日K",
      "interval": "1d",
      "signal": "买入信号",
      "path": "SAR先翻转",
      "ma10_type": 2
    }
  ]
}
```

---

## 七、转折预警（v3.0 新增）

### 7.1 核心框架

SAR↔MA10 双向验证，两条触发路径：

| 路径 | 触发顺序 | 验证方式 |
|------|----------|----------|
| 路径 A | SAR 先翻转 | 验证 MA10 是否满足三类型条件之一 |
| 路径 B | MA10 先满足 | 验证 SAR 是否已同向翻转（历史翻过且未再反向） |

### 7.2 MA10 转折三类型

| 类型 | 条件（多头为例） | 含义 |
|------|-------------|------|
| 类型 1 | 连续转折=2 + 价>MA10 + 开盘价>上周期开盘 | MA10 明确拐头，最稳 |
| 类型 2 | 连续转折=1 + 量>10周期均量 + 价>MA10 | MA10 刚拐头 + 量能爆发 |
| 类型 3 | 连续转折=1 + 量>上周期量>上上周期量 + 价>MA10 | MA10 刚拐头 + 量递增 |

### 7.3 类型 1 中途确认机制

连续转折=1 且不满足类型2/3时，在下一周期半程自动调度单标数据获取：
- 调度时间 = 半程点 - fetch 耗时
- 确认条件：连续转折=2 + 价>MA10 + 开盘价>上周期开盘

### 7.4 使用方式

1. **Web 面板**：点击顶部「转折预警」标签页查看所有当前符合条件的标的
2. **卡片 badge**：监控面板卡片上显示 B/S 转折徽章，点击跳转转折预警页
3. **周期独立性**：日K/4h/1h/15m 四个周期各自独立运行

---

## 八、当前监控标的（100个）

标的由 `gateio_futures_scan.py` 自动从 Gate.io 全量合约中按 **24h 成交额排名** 筛选前 100 名，保存至 `gateio_available_symbols.json`。

当前覆盖 BTC、ETH、SOL、XAU(黄金)、XAG(白银)、XRP、DOGE 等约 88 个加密货币/代币，以及 MU、SNDK、NVDAX、TSLAX 等约 12 个美股/ETF 代币合约。

---

## 九、关键注意事项

### 9.1 数据来源与准确性
- **Gate.io 合约价格 ≠ 真实股价**：美股代币合约（如 AMD_USDT）存在 **2-4% 的系统性溢价**
- **趋势方向可靠**：虽然绝对价格有溢价，但涨跌趋势与真实美股高度一致
- **新合约数据不足**：BILL、SOXL、DRAM、CBRS、QCOM 等日K历史数据较少，需等数据积累

### 9.2 网络环境
- **无需代理**：Gate.io API 在中国大陆可直接访问
- **Binance/OKX/CoinGecko/Yahoo Finance 不可用**：已全面测试确认超时或封禁
- **局域网访问限制**：Web 面板默认绑定 `0.0.0.0`，同一 WiFi 下可访问；跨网络需公网 IP 或云服务器

### 9.3 API 限流
- Gate.io API 有频率限制，脚本已内置 **0.25秒请求间隔**
- 单次全量刷新约 128 次 API 请求，耗时约 45 秒
- 避免短时间内多次点击"刷新数据"

---

## 十、待办事项（按优先级排序）

### P0 - 核心功能扩展
- [ ] **技术指标扩展**：MA5 / MA20 / MA60 / MA120
- [ ] **金叉死叉信号**：MA5 上穿/下穿 MA10，MA10 上穿/下穿 MA20
- [ ] **MACD 指标**：DIF、DEA、MACD 柱状图
- [ ] **RSI 相对强弱指数**：6日/12日/24日 RSI
- [ ] **布林带**：上轨、中轨、下轨

### P1 - 体验优化
- [ ] **前端图表可视化**：集成 lightweight-charts，显示 K 线 + MA 线
- [ ] **历史数据持久化**：SQLite 本地数据库，减少 API 调用、支持回溯
- [x] **趋势转折告警**：当 MA10 从"连续下跌"转为"连续上涨"（或相反）时推送通知（含企业微信）
- [ ] **前端标的管理**：页面直接增删监控标的，无需修改 JSON 文件
- [x] **持仓方向标记**：卡片支持多/空/无三态切换，服务器端持久化
- [x] **预警快捷跳转**：点击顶部预警 badge 自动滚动定位到对应卡片

### P2 - 部署与通知
- [ ] **云服务器部署**：部署到香港/新加坡 VPS，实现 24h 随时访问
- [x] **推送通道**：企业微信机器人 Webhook 已接入
- [ ] **推送扩展**：钉钉/企业微信/Telegram Bot
- [ ] **定时任务**：Windows 任务计划程序 / Linux cron 自动运行
- [x] **自动启动浏览器**：运行脚本后数据刷新完成自动打开面板
- [x] **启动进度反馈**：命令行黑客风格启动画面 + 实时进度条

### P3 - 高级功能
- [x] **转折预警**：SAR↔MA10 双向验证转折预警（v3.0）
- [ ] **多标的对比面板**：横向对比多个标的的趋势强度
- [ ] **数据导出**：Excel / CSV 导出监控结果
- [ ] **回测框架**：基于历史数据验证 MA10 策略有效性

---

## 十一、常见问题

**Q: 为什么有些标的一直显示"数据不足"？**
A: BILL_USDT、SOXL_USDT、DRAM_USDT、CBRS_USDT、QCOM_USDT 是 Gate.io 近期新上线的合约，日K历史数据不足 20 根。等 1-2 周数据积累后自动正常。

**Q: 为什么 AMD_USDT 价格和真实 AMD 股价不一样？**
A: Gate.io 上的美股代币是**股权代币永续合约**，价格由 Gate.io 市场供需决定，并非直接锚定真实股票交易所。实测存在 2-4% 溢价，但趋势方向一致。

**Q: 可以增加 NVDA、TSLA、AMZN 吗？**
A: Gate.io 目前没有这些合约。如需监控，需使用能访问美股数据源的方案（Finnhub/Alpha Vantage），但这些 API 在中国大陆可能也有访问限制。

**Q: 电脑关机后手机还能访问吗？**
A: 不能。当前方案是电脑本地运行 Flask 服务。如需 24h 访问，需部署到云服务器（约 20-50 元/月的香港/新加坡 VPS）。

**Q: 如何修改自动刷新间隔？**
A: 打开 `app.py`，找到 `AUTO_REFRESH_INTERVAL = 300`，将 300 改为需要的秒数（如 60 表示每分钟刷新）。

---

## 十二、更新日志

| 日期 | 版本 | 内容 |
|------|------|------|
| 2026-06-07 | **v3.5.2** | 短周期订阅修复：Path B SAR `sar_flip`→`sar_dir` 方向变化检测；Path A `prev_ma10 is not None` 守卫回退；DOMINANCE_RATIO=3 噪音过滤 |
| 2026-06-07 | **v3.5.1** | 5m/1m 快刷新拆分 |
| 2026-06-06 | **v3.5** | 短周期 5m/1m + 60s 快刷新 + 蜡烛成熟度门控 |
| 2026-06-06 | **v3.4** | SAR 翻转修复 + 企业微信优化 + 噪音过滤 + 类型1条件调整 |
| 2026-06-06 | **v3.3** | 低价格币 MA10 趋势修复 + 各种健壮性加固 |
| 2026-06-05 | **v3.2** | 中途检查 5 项修复：fire_at 对齐 K 线边界、pending 方向改用 MA10 趋势、_do_mid_check tp_state 去重 + SAR 同向验证、旧调度项清理；极偏推送改为仅周期末次刷新推送（精简汇总）+ 去重绑定 candle 起始；API 超时优化（max_retries 1, timeout (10,15)） |
| 2026-06-05 | **v3.1** | 企业微信订阅推送 + 极偏信号v4分周期阈值 + 回测脚本 |
| 2026-06-05 | **v3.0** | 转折预警 v3：SAR↔MA10 双向验证框架（SAR先翻转→MA10验证 / MA10先满足→SAR验证）；MA10 三类型条件（2周期+开盘确认 / 1周期+量爆发 / 1周期+量递增）；类型1 中途提前确认机制；移除旧周期背离；转折预警独立页面；状态持久化 tp_state.json |
| 2026-05-28 | v1.4.2 | 修复前端倒计时与后端刷新调度脱节 |
| 2026-05-28 | v1.4.1 | 24h成交额卡片展示 + 多维度排序 + 修复持仓排序bug |
| 2026-05-27 | v1.4 | 标的扩展至成交额前100 + 并行数据获取 + 搜索过滤 + 持仓下拉 |
| 2026-05-26 | v1.3.2 | 转折预警调度精确到第59分钟 |
| 2026-05-26 | v1.3.1 | 转折预警按周期边界调度 + 状态存储重构 |
| 2026-05-26 | v1.3 | 多周期背离信号 + K线对齐自动刷新 |
| 2026-05-25 | v1.2 | 4小时周期 + 持仓持久化 + 自动启动浏览器 |
| 2026-05-24 | v1.1 | 趋势转折预警 + 企业微信推送 |
| 2026-05-24 | v1.0 | 初始版本：32标的 + 四周期 MA10 + Web 面板 |

---

## 十三、联系与反馈

如需继续完善本项目，向 Claude 说明即可。建议直接说：
- "继续完善 Gate.io 监控项目"
- "帮我加 MACD 指标"
- "部署到云服务器"
- "加微信推送功能"

